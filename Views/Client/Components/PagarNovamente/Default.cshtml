@model ViewPagarNovamente

<div class="text-left">
    <div class="cartoes-slider mt-5">
        @(await Component.InvokeAsync<CartoesViewComponent>(new { IdCliente = Model.IdCliente}))
    </div>

        <div class="checkout-sidebar-price-table mt-30">
            <h5 class="title">Tabela de preços</h5>

            <div class="sub-total-price">
                <div class="total-price">
                    <p class="value">Subtotal do carrinho:</p>
                    <p class="price" menu-total-amount>@Model.Transacao.Subtotal</p>
                </div>
                <div class="total-price shipping">
                    <p class="value">Frete:</p>
                    <p class="price" valor-frete>@Model.Transacao.Frete</p>
                </div>
                <div class="total-price discount">
                    <p class="value">Descontos:</p>
                    <p class="price" total-descontos>R$ 0,00</p>
                </div>
            </div>

            <div class="total-payable">
                <div class="payable-price">
                    <p class="value">Total a pagar:</p>
                    <p class="price" total-compra>@Model.Transacao.Total</p>
                </div>
            </div>
        </div>
</div>